import { defineConfig } from 'vite';

// Конфиг Vite для GitHub Pages
export default defineConfig({
// root можно не указывать, по умолчанию '.'
base: '/travel/', // ВАЖНО: имя репозитория c учётом регистра
});